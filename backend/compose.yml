services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: findependence
      POSTGRES_USER: findependence
      POSTGRES_PASSWORD: local-password
    ports:
      - "5432:5432"
    volumes:
      - findependence-postgres:/var/lib/postgresql/data

volumes:
  findependence-postgres:
